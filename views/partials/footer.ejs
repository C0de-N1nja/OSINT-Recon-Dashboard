</main>
<footer>
    <div class="container">
        <p>© 2024 C0de-N1nja. For Educational & Ethical Use Only.</p>
    </div>
</footer>

<!-- Standard Modal -->
<div id="metadata-modal" class="modal-overlay">
    <div class="modal-content">
        <div class="modal-header">
            <h3>Analysis</h3>
            <button id="modal-close-btn" class="modal-close-btn">&times;</button>
        </div>
        <div id="modal-body" class="modal-body"></div>
    </div>
</div>

<!-- Terminal Loader Modal -->
<div id="terminal-loader-modal" class="modal-overlay">
    <div class="terminal-loader">
        <div class="terminal-header">
            <span class="terminal-title">Executing Scan...</span>
        </div>
        <div class="terminal-output"></div>
    </div>
</div>

<!-- Add these missing styles here for now, we can move them later -->
<style>
    .modal-overlay {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background-color: rgba(0, 0, 0, 0.7);
        display: none;
        justify-content: center;
        align-items: center;
        z-index: 1000;
    }

    .modal-content {
        background-color: var(--color-surface);
        padding: 20px;
        border-radius: 8px;
        border: 1px solid var(--color-border);
        width: 90%;
        max-width: 700px;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.5);
    }

    .modal-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        border-bottom: 1px solid var(--color-border);
        padding-bottom: 10px;
        margin-bottom: 15px;
    }

    .modal-header h3 {
        margin: 0;
        color: var(--color-primary);
    }

    .modal-close-btn {
        background: none;
        border: none;
        font-size: 2rem;
        color: var(--color-text-secondary);
        cursor: pointer;
        padding: 0;
        line-height: 1;
    }

    .modal-body {
        max-height: 70vh;
        overflow-y: auto;
    }

    .metadata-table {
        width: 100%;
        border-collapse: collapse;
    }

    .metadata-table td {
        padding: 8px;
        border-bottom: 1px solid var(--color-border);
    }

    .metadata-table td:first-child {
        font-weight: bold;
        color: var(--color-text-secondary);
        width: 30%;
    }

    .history-event {
        border-bottom: 1px solid var(--color-border);
        padding: 10px 5px;
        margin-bottom: 10px;
    }

    .history-meta {
        font-size: 0.9em;
        color: var(--color-text-secondary);
        margin-bottom: 5px;
    }

    .history-change {
        font-family: var(--font-data);
        font-size: 0.9em;
    }

    .history-old {
        color: #ff7878;
        text-decoration: line-through;
    }

    .history-new {
        color: #89d489;
    }

    .terminal-loader {
        width: 600px;
        background-color: #0d0d0d;
        border: 1px solid var(--color-border);
        border-radius: 5px;
        font-family: var(--font-data);
    }

    .terminal-header {
        background-color: var(--color-border);
        padding: 8px 12px;
        border-top-left-radius: 5px;
        border-top-right-radius: 5px;
        color: var(--color-text-secondary);
    }

    .terminal-output {
        height: 300px;
        padding: 15px;
        overflow-y: auto;
        font-size: 0.9rem;
    }

    .terminal-output p {
        margin: 0 0 5px 0;
        white-space: pre-wrap;
    }

    .terminal-output .data-font {
        color: var(--color-secondary);
    }

    @keyframes blink {
        50% {
            opacity: 0;
        }
    }

    .terminal-output .cursor {
        animation: blink 1s step-end infinite;
    }
</style>

<script src="/js/main.js"></script>
</body>

</html>